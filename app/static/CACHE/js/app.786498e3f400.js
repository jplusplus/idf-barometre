// Generated by CoffeeScript 1.6.2
angular.module('barometre', ['barometreFilters']).config([
  '$interpolateProvider', '$routeProvider', function($interpolateProvider, $routeProvider) {
    $interpolateProvider.startSymbol('[[');
    $interpolateProvider.endSymbol(']]');
    return $routeProvider.when('/', {
      controller: QuestionListCtrl,
      templateUrl: "/partial/questionList.html"
    }).when('/answers/:question-:sample', {
      controller: AnswerGraphCtrl,
      templateUrl: "/partial/answerGraph.html"
    }).otherwise({
      redirectTo: '/'
    });
  }
]).animation("explode-enter", function() {
  return {
    setup: function(element) {
      var distX, distY;

      distX = ~~(500 + Math.random() * 1000) - 1000 + "px";
      distY = ~~(500 + Math.random() * 1000) - 1000 + "px";
      return element.css({
        "opacity": 0,
        "transform": "translate(" + distX + ", " + distY + ")"
      });
    },
    start: function(element, done) {
      return element.animate({
        "opacity": 1,
        "transform": 'translate(0, 0)'
      }, 800, function() {
        return done();
      });
    },
    cancel: function(element, done) {}
  };
});
